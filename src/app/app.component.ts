import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  styles: [`
  `],
  template: `
    <div>home</div>
    <router-outlet></router-outlet>
  `
})
export class AppComponent {
  title = 'app';
}
