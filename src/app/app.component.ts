import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  styles: [`
  `],
  template: `
    <router-outlet dir="rtl"></router-outlet>
  `
})
export class AppComponent {
  title = 'app';
}
