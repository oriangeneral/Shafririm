import { Component } from '@angular/core';

@Component({
  selector: 'login',
  styles: [`
  `],
  template: `
    <div>login</div>
    <button mat-button>Click me!</button>
  `
})
export class LoginComponent {
  title = 'login';
}
