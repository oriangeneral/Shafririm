/*
|--------------------------------------------------------------------------
| APP CONFIGURATION
|--------------------------------------------------------------------------
|
| The values will be inserted when building the app.
|
|
*/
let options = {};

export let config = (option) => options[option];
